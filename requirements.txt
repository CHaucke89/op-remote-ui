# OpenPilot UI Streaming Server Dependencies (host running stream_server.py)
Flask==2.3.3
flask-sock==0.7.0
numpy==1.24.3

# UI-side capture dependencies (openpilot device, alongside pyray).
# pyray is already provided by openpilot; Pillow replaces Qt's JPEG encoder.
Pillow>=10.0.0